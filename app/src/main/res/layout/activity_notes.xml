<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#12201F"
    android:padding="12dp">

    <!-- Label and, when running, the live timer -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/notesLabel"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:textColor="#F1EDE3"
            android:textSize="18sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/notesTimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#E8A33D"
            android:fontFamily="monospace"
            android:textSize="22sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Show runs with no note"
            android:textColor="#8FA39E"
            android:textSize="15sp" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/switchShowEmpty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <!-- History, oldest first so the newest sits beside the input -->
    <ScrollView
        android:id="@+id/notesScroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@drawable/bg_row_idle"
        android:padding="6dp">

        <LinearLayout
            android:id="@+id/notesList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <!-- Current run's note -->
    <TextView
        android:id="@+id/notesInputCaption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="#8FA39E"
        android:textSize="15sp" />

    <EditText
        android:id="@+id/editCurrentNote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="52dp"
        android:maxHeight="140dp"
        android:gravity="top|start"
        android:background="@drawable/bg_row_track"
        android:padding="12dp"
        android:hint="Note for this run"
        android:textColorHint="#5C736E"
        android:textColor="#F1EDE3"
        android:textSize="27sp"
        android:inputType="textMultiLine|textCapSentences"
        android:importantForAutofill="no"
        android:layout_marginTop="4dp" />

    <!-- Export sits beside Done, weighted lighter: it's the occasional
         action and Done is the one reached for every time. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp">

        <Button
            android:id="@+id/btnNotesExport"
            android:layout_width="0dp"
            android:layout_weight="0.38"
            android:layout_height="46dp"
            android:layout_marginEnd="6dp"
            android:text="Export"
            android:textAllCaps="false"
            android:textColor="#A9BDB8"
            android:textSize="14sp"
            android:padding="0dp"
            android:background="@drawable/bg_button_small" />

        <Button
            android:id="@+id/btnNotesDone"
            android:layout_width="0dp"
            android:layout_weight="0.62"
            android:layout_height="46dp"
            android:text="Done"
            android:textAllCaps="false"
            android:textColor="#1A1206"
            android:textSize="14sp"
            android:textStyle="bold"
            android:background="@drawable/bg_button_work" />
    </LinearLayout>

</LinearLayout>
